<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="OrderProcessing_WinUI.OrderControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OrderProcessing_WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <!--This will contain template for single order
        and Order details will display this user control in details-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <TextBox Grid.Column="0" Text="{Binding Name}"/>
        <TextBox Grid.Column="1" Text="{Binding BookTitle}"/>
        <TextBox Grid.Column="2"/>
        <TextBox Grid.Column="3"/>
        <TextBox Grid.Column="4"/>


    </Grid>
</UserControl>
